//  All same to same, just remeber this, if there are a lot of operators of same precedence, then compute left 
// to right based on associativity.

var dogAge  = prompt("Enter the dog Age");
var humanAge = (dogAge - 2) * 2 + 21;
alert(humanAge);